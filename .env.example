# Database - PostgreSQL connection string
# For DigitalOcean Managed PostgreSQL, get this from your database settings
DATABASE_URI=postgresql://user:password@host:25060/database?sslmode=require

# Payload CMS Secret - Generate a secure random string (min 32 characters)
# You can generate one with: openssl rand -hex 32
PAYLOAD_SECRET=your-secure-secret-key-min-32-characters

# Server URL - Your production domain
NEXT_PUBLIC_SERVER_URL=https://yourdomain.com
PAYLOAD_PUBLIC_SERVER_URL=https://yourdomain.com

# Node environment
NODE_ENV=production
